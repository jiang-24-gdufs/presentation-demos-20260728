# Cesium 新人技术分享 · 产出物清单与编写指南

> 本指南旨在明确本次技术分享需产出的**两份核心交付物**：  
> 1. **一份单页 HTML 讲解文档**（承载演讲核心内容，用作幻灯片替代或补充）。  
> 2. **若干个独立 HTML 示例代码**（每个演示一个核心功能，可独立运行，支持通过 CDN 或本地 Build 目录加载 Cesium / 超图 Cesium）。  
> 
> 所有文件均遵循 **“讲用法不讲源码，讲场景不讲原理”** 原则，不涉及底层渲染或复杂算法分析。

---

## 一、核心讲解文档（`index.html`）

### 1.1 定位
- 一份**自包含的单页网页**，通过滚动或点击切换章节，充当演讲幻灯片。  
- 内容紧扣五大核心模块，**仅保留关键点、数据表格、决策树和避坑提示**，可少量使用生活化类比，但以技术性表达为主。  
- 视觉风格：简洁、技术向、适合大屏投映，配色参考 Cesium 官方（深蓝 + 白）。

### 1.2 页面结构（建议）

| 章节 | 内容要点 |
|------|----------|
| **标题页** | 分享主题、主讲人、日期，可附加一句话定位。 |
| **模块一：数据类型全景图** | 5 种数据类型表格（格式、加载方式、适用场景），附“倾斜摄影必须流式加载”警示。 |
| **模块二：Entity API** | 增删改查流程 + 性能红线（<100 个用 Entity，>100 个用 Primitive），提供选型决策树（文字描述）。 |
| **模块三：属性查询** | 点击拾取流程（事件→拾取→分支判断→UI 反馈），涵盖模型基础信息查询与地理空间信息查询；附“大数据只取 ID，后端查详情”建议。 |
| **模块四：调试与体检** | Inspector 工具介绍 + Network/Console 面板用法，附常见症状自查表（白屏、黑屏、悬空、跨域等）。 |
| **模块五：性能优化** | 三步诊断（FPS→预警→调参），附“傻瓜式优化清单”和 SSE 参数说明。 |
| **总结** | 强调“数据从哪来、画点去哪找、卡了去哪调”三个核心落脚点。 |
| **答疑页** | 万能话术占位，显示保命台词，引导现场提问。 |

---

## 二、示例代码文件（`demo-*.html`）

> 共 **5 个独立示例**，每个对应一个模块的核心操作，可直接在浏览器中运行（需联网加载 Cesium 库和瓦片，或通过本地 Build 目录引用超图 Cesium）。

### 2.1 示例清单

| 文件名 | 对应模块 | 演示内容 | 关键操作 |
|--------|----------|----------|----------|
| `demo-01-loading.html` | 数据类型 | 加载影像底图 + 地形 + 3D Tiles（倾斜摄影） | 展示三种数据源叠加，打开 Inspector 观察瓦片加载过程。 |
| `demo-02-entity.html` | Entity API | 添加带图标和标签的实体，并绑定业务属性（ID/状态） | 使用 `viewer.entities.add`，并在控制台演示 `getById` 查询。 |
| `demo-03-pick.html` | 属性查询 | 点击 Entity 弹出属性详情，点击 3D Tiles 要素弹出批量属性 | 实现 `screenSpaceEventHandler`，区分 `entity` 与 `tile` 拾取逻辑。 |
| `demo-04-debug.html` | 调试 | 开启/关闭 Inspector，并演示控制台打印实体列表 | 包含 `viewer.extend(Cesium.Inspector)`，显示 FPS 和包围盒。 |
| `demo-05-performance.html` | 性能优化 | 动态调整 SSE 参数，观察帧率变化；开启请求渲染模式 | 添加滑块控制 `maximumScreenSpaceError`，实时显示 FPS。 |

### 2.2 每个示例的通用要求
- **不含多余注释代码**（仅在页面中显示必要的操作指引文本）。  
- **支持两种加载方式**：  
  - 通过 CDN 加载标准 Cesium（如 `https://cesium.com/downloads/cesiumjs/releases/`）。  
  - 通过本地 Build 目录加载超图 Cesium（用于预览 S3M 数据）。  
- **必须提供 Cesium Ion 令牌占位**（用户需自行替换为自己的令牌）。  
- 每个页面顶部用大字标题说明当前演示模块，底部附“返回索引”链接（可跳回讲解文档或示例列表）。

---

## 三、附加文档（建议一并产出）

- `README.md`：说明如何运行这些 HTML（本地服务器建议、令牌配置、CDN 切换方式等）。
---

## 四、编写检查清单

- [ ] 讲解文档不包含任何可执行代码，仅使用 API 名称和流程描述，且所有超链接正确指向对应的示例文件。  
- [ ] 每个示例均可独立运行，演示结果直观可见（界面简洁，重点突出功能，不追求花哨样式）。  
- [ ] 性能红线和避坑提示在讲解文档和示例中均以醒目方式呈现（如背景色、边框或图标标记）。  
- [ ] 所有 HTML 文件遵循语义化标签，确保在不同浏览器下基本兼容。

